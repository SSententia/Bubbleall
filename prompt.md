Audit the last commit against @old-plan-corrected.md to see if everything is implemented correctly.


A few noticeable bugs and potential improvements when I reviewed it manually on my phone, ranked by severity:



1\. The Restart bubble feature doesn't seem to work. Nothing happens when I click the restart icon/button.



2\. The built-in keyboard is blocking content. Should only appear when focused on a field inside the browser. It should also move the browser away to not block content when it does appear, just like the system's keyboard.



3\. The built-in keyboard's layout is very unusual and very difficult to type on. At least make it like a conventional keyboard where the space is in the middle, etc. I am quite familiar with Gboard (Google's keyboard layout).



4\. The transparency slider still doesn't work in real time. Manually restarting the bubble workaround still works.



5\. Switching from "Keyboard" mode to one of the three modes that is supposed to hide the system keyboard ("built in", "paste", "none") without restarting cannot hide the system keyboard. After restart, the system keyboard is fully hidden. However, switching back fails to bring back the keyboard, which is resolved by another restart.



6\. "Built-in keyboard" and "Paste" mode are destroyed when I open a new tab. Going to the home screen (where the "Web", "Video", and "Music" reside) and going back restores it.



Let me know if you discover more.

